node_modules/
npm-debug.log*

# Рабочий конфиг панели (создаётся при первом запуске из config.default.json)
config/config.json

# Локальные логи панели
logs/
*.log

# Сгенерированный .bat запуска сервера (генерируется автоматически)
generated/

# Рабочие данные панели: тайлы карты, картинка карты и — главное — аккаунты
# с хешами паролей. Ничего из этого в репозитории быть не должно.
data/
