{
  "name": "dayz-panel",
  "version": "1.0.0",
  "description": "Локальная веб-панель управления сервером DayZ Standalone (Windows)",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "check": "node -e \"const{execFileSync}=require('child_process');const{readdirSync,statSync}=require('fs');const walk=d=>readdirSync(d).flatMap(f=>{const p=d+'/'+f;return statSync(p).isDirectory()?walk(p):p.endsWith('.js')?[p]:[]});for(const f of walk('src'))execFileSync(process.execPath,['--check',f],{stdio:'inherit'});console.log('синтаксис в порядке')\"",
    "check:mod": "node tools/check-enforce.js mod/DayZPanelBridge"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mysql2": "^3.23.3"
  },
  "license": "MIT",
  "private": true
}
